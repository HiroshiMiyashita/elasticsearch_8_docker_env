# Start Elasticsearch Docker Envrionment.

1. Start Docker Service

    sudo service docker start

2. Start elasticserch service defined in docker-compose.yaml file.

    docker run -d --name elasticsearch -p 9200:9200 -p 9300:9300 -e "discovery.type=single-node" elasticsearch:8.2.3
